# ProteinOptimizer-specific Python dependencies
# Assumes the parent sde-harness requirements (torch, transformers, numpy,
# litellm, weave, openai, anthropic, ...) are already installed. See
# ../../requirements.txt for that base set, or use the project-level
# environment.yml which installs both layers.

pandas>=1.3.0
matplotlib>=3.5.0
seaborn
omegaconf

# MolLEO specific requirements
# Note: Some dependencies are already satisfied in ../../requirements.txt

# PyTorch and related (torch>=2.0.0 already in parent requirements)
# Only specify if specific version needed for MolLEO
# pytorch==2.1.2
# torchvision==0.16.2
# torchaudio==2.1.2

# Chemistry and molecular libraries
PyTDC
rdkit

# Configuration
PyYAML

# Language models (transformers>=4.30.0 already in parent requirements)
sentencepiece
selfies

# Note: The following are already satisfied in ../../requirements.txt:
# - torch>=2.0.0
# - transformers>=4.30.0
# - numpy>=1.21.0
# - openai>=1.0.0 (for GPT-4 API)